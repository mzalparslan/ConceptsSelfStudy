#pragma once

// Shared, running counter for the "TEST#N#" output prefix, used across
// all topic files so the whole run is numbered continuously (not per file).
// Defined once in ConceptsSelfStudy.cpp.
extern int testCount;
